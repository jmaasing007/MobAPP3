import 'package:flutter/material.dart';

class NoteRowWidget extends StatelessWidget {
  const NoteRowWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      Icon(Icons.ac_unit.)
    ],);
  }
}
